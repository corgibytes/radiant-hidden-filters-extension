namespace :radiant do
  namespace :extensions do
    namespace :hidden_filters do
      task :update do
        # only here to keep the install script from failing
      end
      
      task :migrate do
        # only here to keep the install script from failing
      end
    end
  end
end

